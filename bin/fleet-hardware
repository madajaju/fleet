#!/usr/bin/env node

var program =  require('commander');

program
  .version("0.0.1")
  .command('add [options]', 'Add hardware to the Data Center')
  .command('list [options]', 'List hardware in the Data Center')
  .command('remove [options]', 'Remove hardware in the Data Center')
  .parse(process.argv);

